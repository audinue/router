{
  "name": "@audinue/router",
  "version": "1.0.0",
  "main": "src/index.mjs",
  "type": "module",
  "scripts": {
    "dev": "serve -s",
    "build": "esbuild --bundle --minify --sourcemap --format=esm --outfile=index.js src/index.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "serve": "^14.2.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
